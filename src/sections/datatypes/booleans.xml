<section xmlns="http://docbook.org/ns/docbook" version="5.0" xml:id="section.booleans"
    xmlns:xi="http://www.w3.org/2001/XInclude" >
    <info>
        <title>Booleans</title>
    </info>

    <para>
        The boolean type is a primitive data type and actually the most simple type because it has only two possible values: <literal>true</literal> and <literal>false</literal>. Both <literal>true</literal> and <literal>false</literal> are also literal boolean values, and <varname>boolean</varname> is the name of the type. For example, you can declare and initialize a boolean variable as follows:

<programlisting>
boolean mybool = true;
</programlisting>

        or simply

<programlisting>
var mybool = true;
</programlisting>

    </para>

</section>
