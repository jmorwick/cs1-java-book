var pointsInGame1 = 6;
var pointsInGame2 = 8;
var pointsInGame3 = 8;
var averagePoints = (int)(
						(pointsInGame1 + // find total points
						 pointsinGame2 + 
						 pointsInGame3) / 3.0 // average them
							+ 0.5); // round up to nearest whole number