<example xmlns="http://docbook.org/ns/docbook" version="5.0" xml:id="ex.call-sqrt"
    xmlns:xi="http://www.w3.org/2001/XInclude">
    <title>Calling the Square Root Method</title>

    <para>Start by importing all of the static methods in the <code>Math</code> class:
        <programlisting>import static java.lang.Math.*;</programlisting>
        Note that <varname>Math</varname> must be capitalized and the line should end in a semicolon. Once you've typed this in, you're allowed to make calls to the square root function and several others at will. For example:
        <programlisting>sqrt(4)</programlisting>
        This expression should yield the value <literal>2.0</literal>. Note that the method returns a <type>double</type> value, even though <literal>4</literal> is a perfect square. Try calling this method with some other, messier values. </para>
    <para>A list of all the other methods you can use after the import statement above can be found here: <link>https://docs.oracle.com/javase/10/docs/api/java/lang/Math.html</link></para>
</example>
